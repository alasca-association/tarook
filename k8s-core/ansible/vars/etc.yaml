---
cluster_repo_dir: "{{ (ansible_inventory_sources[0] | dirname) }}/../../"
etc_dir: "{{ (cluster_repo_dir | dirname) }}/etc"
config_dir: "{{ (cluster_repo_dir | dirname) }}/config"
ssh_known_hosts_file: "{{ etc_dir }}/ssh_known_hosts"
...
