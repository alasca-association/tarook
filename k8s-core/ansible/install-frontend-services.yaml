---
- name: Install & configure frontend services
  hosts: frontend
  gather_facts: true
  vars_files:
  - vars/disruption.yaml
  - vars/auto_generated_preamble.yaml
  - vars/etc.yaml
  - vars/retries.yaml
  - vars/vrrp-common.yaml
  roles:
  - role: keepalived
    tags: keepalived
  - role: haproxy
    tags: haproxy
...
