---
- name: Renewal Kubernetes client certificate
  hosts: masters
  gather_facts: false
  vars_files:
    - vars/disruption.yaml
    - vars/auto_generated_preamble.yaml
    - vars/etc.yaml
    - vars/retries.yaml
    - vars/vault-config.yaml
  tasks:
    - name: Renewal Kubernetes client certificate
      ansible.builtin.include_role:
        name: k8s-master
        tasks_from: obtain-kubeconfigs
...
