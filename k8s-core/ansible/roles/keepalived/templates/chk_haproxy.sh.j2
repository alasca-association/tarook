#!/usr/bin/env bash

{{ _auto_generated_preamble }}

{% if ipv4_enabled | bool %}
wget http://127.0.0.1:6669/healthz -O - 2>&1 | grep -q '200 OK'
{% endif %}
{% if ipv6_enabled | bool %}
wget -6 http://[::1]:6669/healthz -O - 2>&1 | grep -q '200 OK'
{% endif %}
