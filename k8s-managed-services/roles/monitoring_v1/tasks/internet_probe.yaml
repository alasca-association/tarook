- name: apply manifests
  kubernetes.core.k8s:
    state: present
    apply: yes
    definition: "{{ lookup('template', 'internet_probe.yaml') }}"
    validate:
      fail_on_error: yes
      strict: yes
