---
- name: restart thanos-store statefulset
  ansible.builtin.command:
  args:
    argv:
    - kubectl
    - -n
    - "{{ monitoring_namespace }}"
    - rollout
    - restart
    - statefulset
    - thanos-store

- name: restart prometheus-k8s statefulset
  ansible.builtin.command:
  args:
    argv:
    - kubectl
    - -n
    - "{{ monitoring_namespace }}"
    - rollout
    - restart
    - statefulset
    - prometheus-k8s
...
