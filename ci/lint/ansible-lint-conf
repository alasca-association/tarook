use_default_rules: true

skip_list:
  - role-name      # Role name {} does not match ``^[a-z][a-z0-9_]+$`` pattern
  - name[casing]
  - name[template]
  - name[missing]
  - experimental   # all rules tagged as experimental
  - yaml           # we run yamllint on everything anyway, so catch those issues there!
