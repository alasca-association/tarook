---
- name: restart thanos-store
  become: true
  ansible.builtin.command:
  args:
    argv:
    - kubectl
    - rollout
    - restart
    - statefulset
    - "-n"
    - "{{ monitoring_namespace }}"
    - thanos-store

- name: restart prometheus-k8s
  become: true
  ansible.builtin.command:
  args:
    argv:
    - kubectl
    - rollout
    - restart
    - statefulset
    - "-n"
    - "{{ monitoring_namespace }}"
    - prometheus-k8s
...
