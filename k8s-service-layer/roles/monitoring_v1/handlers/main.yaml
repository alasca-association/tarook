---
- name: restart thanos-store
  become: yes
  command:
  args:
    argv:
    - kubectl
    - rollout
    - restart
    - statefulset
    - "-n"
    - "{{ monitoring_namespace }}"
    - thanos-store

- name: restart prometheus-k8s
  become: yes
  command:
  args:
    argv:
    - kubectl
    - rollout
    - restart
    - statefulset
    - "-n"
    - "{{ monitoring_namespace }}"
    - prometheus-k8s
...
