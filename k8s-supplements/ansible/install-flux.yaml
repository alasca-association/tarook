---
- name: Install Flux
  hosts: orchestrator
  gather_facts: false
  vars_files:
  - vars/retries.yaml
  roles:
  - role: fluxcd2_v1
    when: (fluxcd_enabled is defined) and (fluxcd_enabled | bool)
    tags:
    - fluxcd2_v1
...
