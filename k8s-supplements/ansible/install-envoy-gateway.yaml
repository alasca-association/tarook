---
- name: Install Envoy Gateway
  hosts: orchestrator
  gather_facts: false
  vars_files:
  - vars/retries.yaml
  - vars/disruption.yaml
  roles:
  - role: envoy-gateway
    when: envoy_gateway_enabled | bool
    tags:
    - envoy-gateway
...
