Generation of kubeconfigs of control plane components has been adjusted such that they talk to the local kube-apiserver instead of using the load-balanced virtual IP as server backend.
The change will take effect on their next renewal which happens on a Kubernetes upgrade or on a rollout if the certificates used are less than 4 weeks valid or more than 16 weeks old.
