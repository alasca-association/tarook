---
- name: Run kubeadm upgrade node  # noqa no-changed-when
  when: do_upgrade
  become: true
  ansible.builtin.command:
    argv:
      - "kubeadm"
      - "upgrade"
      - "node"
      - "--patches"
      - "/etc/kubernetes/kubeadm-patches"
...
