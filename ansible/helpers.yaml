# Random collection of tasks helping the admin

- name: Manage SSH-Keys
  hosts: all
  tags:
    - public_key
  tasks:
    - name: Add public key to all nodes
      authorized_key:
        user: "{{ ansible_ssh_user }}"
        state: present
        key: "{{ item }}"
      loop: "{{ ssh_public_keys }}"
