---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: shared-fs-test
  namespace: default
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 2Gi
  storageClassName: "{{ fs_storage_class }}"
