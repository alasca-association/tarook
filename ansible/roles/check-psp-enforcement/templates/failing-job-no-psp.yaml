---
apiVersion: batch/v1
kind: Job
metadata:
  name: failing-job-no-psp
  namespace: "{{ test_check_psp_enforcement_namespace }}"
spec:
  template:
    spec:
      containers:
      - image: debian
        name: debian
        command: ["true"]
      restartPolicy: Never
  backoffLimit: 1
