---
- name: install bird
  apt:
    name: bird
    state: present
  tags:
  - bird

- name: configure bird
  template:
    src: bird.conf
    dest: /etc/bird/bird.conf
    owner: bird
    group: bird
    mode: 0o640
  notify: restart bird
  tags:
  - bird

- name: disable bird6
  service:
    name: bird6
    enabled: no
    state: stopped
  tags:
  - bird
