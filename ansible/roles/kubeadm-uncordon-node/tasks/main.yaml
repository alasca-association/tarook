- name: Uncordon the worker node
  become: no
  environment:
    KUBECONFIG: /etc/kubernetes/admin.conf
  delegate_to: "{{ (groups['masters'] | sort)[0] }}"
  command:
  args:
    argv:
    - kubectl
    - uncordon
    - "{{ inventory_hostname }}"
