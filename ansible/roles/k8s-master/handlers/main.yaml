- name: Restart kubelet
  delegate_to: "{{ item }}"
  loop: "{{ groups['k8s_nodes'] }}"
  become: yes
  run_once: yes
  service:
    name: kubelet
    state: restarted

- name: Publish join key
  service:
    name: publish-join-key.service
    state: started
